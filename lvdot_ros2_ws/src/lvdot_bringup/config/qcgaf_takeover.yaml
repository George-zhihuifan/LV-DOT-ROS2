model:
  cam_dim: 9
  lidar_dim: 10
  quality_dim: 7
  hidden_dim: 20
  feat_dim: 32
  output_dim: 7
  max_cam_dets: 16
  max_lidar_dets: 16

ros:
  cam_topic: "/onboard_detector/visual_bboxes"
  lidar_topic: "/onboard_detector/lidar_bboxes"
  color_topic: "/rgbd_camera/image"
  depth_topic: "/rgbd_camera/depth_image"
  yolo_topic: "/yolo_detector/detected_bounding_boxes"
  lidar_cloud_topic: "/livox/lidar/pointcloud"
  output_topic: "/onboard_detector/filtered_bboxes_qc"
  sync_slop: 0.12
